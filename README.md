# machine_learning

## Python Virtual Environment Setup

This project uses a Python 3.13 virtual environment located in the `ml/` directory.

### Activating the Virtual Environment

To install and activate the virtual environment, run:

```bash

python3.13 -m venv ml
source ml/bin/activate # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

